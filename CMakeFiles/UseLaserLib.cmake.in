#
# This module is provided as LaserLib USE_FILE by LaserLibConfig.cmake.  It can
# be INCLUDEd in a project to load the needed compiler and linker
# settings to use Utility Functions.
#

IF(NOT LaserLib_USE_FILE_INCLUDED)
  SET(LaserLib_USE_FILE_INCLUDED 1)


  # Add compiler flags needed to use LaserLib.
  #SET(CMAKE_C_FLAGS "${CMAKE_C_FLAGS} ${LaserLib_REQUIRED_C_FLAGS}")
  #SET(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} ${LaserLib_REQUIRED_CXX_FLAGS}")
  #SET(CMAKE_EXE_LINKER_FLAGS "${CMAKE_EXE_LINKER_FLAGS} ${LaserLib_REQUIRED_EXE_LINKER_FLAGS}")
  #SET(CMAKE_SHARED_LINKER_FLAGS "${CMAKE_SHARED_LINKER_FLAGS} ${LaserLib_REQUIRED_SHARED_LINKER_FLAGS}")
  #SET(CMAKE_MODULE_LINKER_FLAGS "${CMAKE_MODULE_LINKER_FLAGS} ${LaserLib_REQUIRED_MODULE_LINKER_FLAGS}")

  # Add include directories needed to use LaserLib.
  INCLUDE_DIRECTORIES(${LaserLib_INCLUDE_DIRS})

  # Add link directories needed to use LaserLib.
  LINK_DIRECTORIES(${LaserLib_LIBRARY_DIRS})

ENDIF(NOT LaserLib_USE_FILE_INCLUDED)
